// Throwaway module — isolated from the main project so Fyne deps
// don't pollute go.mod / go.sum until the decision is made.
// Delete the whole testeFyne/ folder once the test is complete.

module testeFyne

go 1.22
