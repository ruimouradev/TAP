module the-answer-protocol

go 1.22

// GUI uses Fyne (Go-native, cross-platform).
// Before implementing cmd/gui, run:
//   go get fyne.io/fyne/v2
// On Linux this also requires CGO and the system graphics dev headers
// (libgl1-mesa-dev + xorg-dev on Debian/Ubuntu). See README.
